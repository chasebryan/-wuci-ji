# Guix Build Room

Execution class: `store-aware-build-room`

This directory defines the controlled measuring chamber for the Guix candidate. Guix daemon and store behavior are blocked by default.

Blocked until an explicit Guix daemon/store policy exists, a controlled backend is usable, and a future explicit build attempt authorization exists.

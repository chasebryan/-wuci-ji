# Debian Minimal Build Room

Execution class: `direct-rootfs-image-build-room`

This directory defines the controlled measuring chamber for the Debian minimal candidate. It does not run debootstrap or fakechroot and does not provide substrate evidence.

Blocked until a controlled debootstrap or rootfs strategy exists, a controlled container backend is usable, and a future explicit build attempt authorization exists.

# Buildroot Build Room

Execution class: `direct-rootfs-image-build-room`

This directory defines the controlled measuring chamber for the Buildroot candidate. It does not attempt a Buildroot build and does not provide substrate evidence.

Blocked until local Buildroot source or an approved source acquisition policy exists, a controlled container backend is usable, and a future explicit build attempt authorization exists.

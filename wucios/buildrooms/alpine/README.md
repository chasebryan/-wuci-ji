# Alpine Build Room

Execution class: `direct-rootfs-image-build-room`

This directory defines the controlled measuring chamber for the Alpine candidate. It does not run `apk`, build a rootfs, or provide substrate evidence.

Blocked until a controlled apk/rootfs strategy exists, a controlled container backend is usable, and a future explicit build attempt authorization exists.

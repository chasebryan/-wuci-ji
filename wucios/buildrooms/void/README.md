# Void Build Room

Execution class: `direct-rootfs-image-build-room`

This directory defines the controlled measuring chamber for the Void candidate. XBPS tooling presence is recorded only as readiness evidence, not as a substrate decision.

Blocked until a controlled XBPS rootfs strategy exists, a controlled container backend is usable, and a future explicit build attempt authorization exists.

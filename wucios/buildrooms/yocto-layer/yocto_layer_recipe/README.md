# Yocto Layer/Recipe Preparation Policy

This directory records Phase 3C-D policy for the Yocto layer/recipe candidate.

Phase 3C-D does not run BitBake, initialize a build environment, run devtool or kas, clone or download layers, generate rootfs or image outputs, select a substrate, rank candidates, or generate a numeric WuciOS score.

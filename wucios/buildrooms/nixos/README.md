# NixOS Build Room

Execution class: `store-aware-build-room`

This directory defines the controlled measuring chamber for the NixOS candidate. Store-aware behavior is blocked by default.

Blocked until an explicit host-store policy or isolated store policy exists, a controlled backend is usable, and a future explicit build attempt authorization exists.

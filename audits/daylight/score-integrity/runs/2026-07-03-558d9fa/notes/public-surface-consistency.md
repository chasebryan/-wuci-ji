# Public-Surface Consistency

The generated public-surface comparison report records `8` claim entries and
marks each entry consistent.

The site validator binds the public v17 score surface to
`site/daylight-status.json` and the committed v17 scorecard digest. It also
binds the public v20 score surface to
`site/daylight-v20-aperture-singularity-status.json` and the committed v20
capsule digest, score, omega, blockers, and non-claims.

The copied public-surface report lists each location found by the audit tool.

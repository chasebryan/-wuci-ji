# Validation

The source score-integrity reports for this run were generated by:

```sh
make daylight-score-integrity-audit
```

The generated final report returned:

```text
result: pass
score_claims_seen: 8
score_claims_recomputed: 3
score_claims_evidence_matched: 3
score_claims_non_claim: 2
failures: 0
unresolved: 0
```

The copied reports are checked by:

```sh
make daylight-score-integrity-audit-directory-check
```

The directory check verifies required files, JSON parsing, manifest report
hashes, `SHA256SUMS.txt`, non-claim boundary flags, and local/private pattern
absence under this run directory.
